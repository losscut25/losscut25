package chap06;
//필드 자동 초기화
//기본타입(primitive type) : byte, short, int, long, float, double, char, boolean)
public class P228 {
	// 필드
	//[접근제한자][제어자] 타입 필드명[=초기값];
	
	String color;
	int price;
	byte by;
	short sh;
	long lo;
	float fl;
	double db;
	char ch;
	boolean bl;
	
	// 생성자
	// 생성자는 객체를 사용하기 위해 필드에서 가져온 데이터 값을 사용할 수 있도록 객체를 지정하는 것이다.(데이터 초기화)
	// 클래스명 참조변수 = new 클래스명(){}
	
	// 메소드
	// 메소드는 생성자에서 만들어진 객체들 중 기능, 방법을 정의한다고 할 수 있는 공간으로 void method의 형태로 만들어간다.
	// [접근제한자][제어자] 메서드명(매개변수리스트){}

}
